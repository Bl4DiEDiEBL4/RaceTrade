{
  "//": "The port here must match Web:Port in appsettings.json. Program.cs configures Kestrel explicitly (so the same binding rules apply however the app is launched), which overrides applicationUrl - if these disagree, VS opens a browser on a port nothing is listening on.",
  "profiles": {
    "RaceTrade.Web": {
      "commandName": "Project",
      "launchBrowser": true,
      "applicationUrl": "http://127.0.0.1:8420",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      },
      "dotnetRunMessages": true
    }
  }
}
